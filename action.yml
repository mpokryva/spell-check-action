name: 'Hello World'
description: 'Greet someone'
inputs:
  text:
    description: 'The input text'
    required: true
runs:
  using: "composite"
  steps:
    - run: echo  ${{ inputs.text }}.
      shell: bash
